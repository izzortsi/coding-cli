{
  "name": "coding-cli",
  "version": "0.2.0",
  "description": "Interactive CLI for multi-turn LLM conversations with tool use, staged writes, and a lisp runtime. Providers: Anthropic (API + OAuth), z.ai, Ollama, LM Studio.",
  "type": "module",
  "main": "dist/index.js",
  "bin": { "coding-cli": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "typescript": "^5.7.3"
  }
}
